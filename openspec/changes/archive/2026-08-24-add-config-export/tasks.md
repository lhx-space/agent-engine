## 1. 导出逻辑

- [x] 1.1 新增 `apps/web/src/lib/export-config.ts`（YAML / JSON 序列化 + Blob 下载）
- [x] 1.2 文件名 `<name>.yaml` / `<name>.json`

## 2. UI 接入

- [x] 2.1 `App.tsx` header 加「导出配置」下拉按钮（YAML / JSON）

## 3. 验证

- [x] 3.1 web typecheck + build 通过
- [x] 3.2 lint / spell / format 通过
