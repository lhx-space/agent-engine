/**
 * @agent-engine/core —— Agent 内核执行引擎。
 * 承载 Agent Loop、多 Agent 编排、插件系统、Hook 管线、Rule 引擎等装配逻辑。
 * 实现见 AGENTS.md 里程碑 M1~M3，通过 OpenSpec change 逐步落地。
 */
export const name = '@agent-engine/core';
export const version = '0.1.0';
