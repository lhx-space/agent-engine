/**
 * @agent-engine/config —— 配置加载与 Schema。
 * 将 YAML / JSON / TypeScript 三种格式的配置归一化为同一份 `AgentConfig`（Zod 校验）。
 * 实现见 AGENTS.md 里程碑 M1，通过 OpenSpec change 逐步落地。
 */
export const name = '@agent-engine/config';
export const version = '0.1.0';
