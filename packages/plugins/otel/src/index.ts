/**
 * @agent-engine/plugin-otel —— OpenTelemetry 可观测插件。
 * 通过 PluginContext 注入 hooks，把 Agent 执行链路接入 OTel。
 * 实现见 AGENTS.md 里程碑 M5。
 */
export const name = '@agent-engine/plugin-otel';
export const version = '0.1.0';
