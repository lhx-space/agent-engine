# agent-engine 宿主的工作区根目录。
# 是 files.roots / sandbox.workspaceRoot 里 `${WORKSPACE_ROOT}` 的插值源。
# 本地：指向你的仓库路径；云端：指向容器挂载点（如 /workspace）。
# 复制本文件为 .env.local 并改成你的实际路径（.env.local 已被 gitignore）。
WORKSPACE_ROOT=/path/to/your/agent-engine
